# PRD — 지역맞춤 귀농 작물 추천 서비스 (가칭: 어디심)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-07-23 |
| 상태 | Draft |
| 작성자 | Jongwon (team_beomin_) |

---

## 1. 개요 (Executive Summary)

귀농을 준비하는 사람이 **지도에서 지역을 고르면**, 그 지역의 **실제 기후·토양 데이터**를 5종 작물(오이·사과·배·상추·감자)의 **표준 생육 조건**과 대조해 **적합도 점수**를 매기고, 가장 잘 맞는 작물을 **추천 이유와 함께** 제시하는 서비스. 여기에 **재배 위험요소 안내 챗봇**과 **작물별 농사 캘린더**를 더해, "전국 공통 가이드"가 아니라 **"내 땅에 맞는 농사 매뉴얼"**을 제공한다.

---

## 2. 배경 & 문제 정의

- 농촌진흥청 등의 **표준 재배 가이드라인은 존재**하지만, 전국 공통 기준이라 **특정 지역의 실제 기후·토양에 맞춘 실행형 매뉴얼은 없다.**
- 귀농 초보자는 "이 동네에서 **무엇을**, **왜**, **언제** 심어야 하는지"를 스스로 판단하기 어렵다.
- 기상·토양·작물 표준 데이터는 각각 공공 API로 흩어져 있어, 일반인이 **통합·해석**하기 어렵다.

> **핵심 가치**: 지역 선택 → 데이터 기반 작물 추천(+이유) → 위험요소 안내 → 실행 캘린더까지, "지역 맞춤 농사 매뉴얼"을 원스톱으로 제공.

---

## 3. 목표

### 3.1 제품 목표
- 사용자가 **선택한 지역**에 대해 5종 작물의 **적합도 점수(0~100)**와 **순위**를 제시.
- 각 추천에 대해 **정량적 근거(어떤 항목이 왜 높/낮은지)**를 자연어로 설명.
- 재배 **위험요소·유의점**을 챗봇으로 질의응답.
- 작물별 **월별 농사 업무 캘린더** 제공.

### 3.2 비목표 (Non-goals, v1 제외)
- 5종 외 작물 확대(v1은 오이·사과·배·상추·감자로 한정).
- 실시간 병해충 예찰, 드론/IoT 센서 연동.
- 실거래 토지 매물·보조금 정보 제공.
- 수익성(경제성) 시뮬레이션.

---

## 4. 대상 사용자 (Persona)

| 페르소나 | 특징 | 니즈 |
|---|---|---|
| **귀농 준비 30대 직장인** | 도시 거주, 농사 무경험, 스마트폰 능숙 | "어디에 뭘 심어야 실패 안 할까?" 데이터로 확인 |
| **은퇴 후 귀농 50대** | 고향/특정 지역 정착 예정, 디지털 리터러시 보통 | 지역 정해짐 → "이 땅에 맞는 작물과 일정" |
| **주말농장/소규모 40대** | 텃밭 수준, 실패 부담 적게 시작 | 쉬운 작물 추천 + 캘린더 알림 |

**공통**: 20~50대, 특정 지역을 이미 염두에 두었거나 탐색 중, **"신뢰할 수 있는 근거"**를 원함.

---

## 5. 핵심 기능 (Features & Priority)

우선순위: **P0(MVP 필수) / P1(중요) / P2(향후)**

### F-1. 지역 선택 (P0) — 필수기능
- **네이버 지도 API**로 지도에서 위치 선택 + 주소 검색.
- 선택 좌표/주소 → **읍·면·동 단위**로 확정 → **법정동코드(10자리)**로 변환.
- 변환은 로컬 법정동코드 CSV(`bjd_lookup`)로 **API 키 없이 즉시** 처리.

### F-2. 지역 환경 데이터 수집 (P0)
- **기상**: 선택 지역 **최근접 ASOS 관측소** 매핑 → 최근 10년 평년값(연평균·생육기·결실기 기온, 30℃초과일수, 연강수량, 연일조시간).
- **토양**: 법정동코드로 **농경지화학성 통계**(pH·유기물·유효인산·칼륨·칼슘·마그네슘) 조회, 지목(밭/과수/논/시설)별 등급 분포.

### F-3. 작물 적합도 점수 & 추천 (P0)
- 5종 작물 각각에 대해 **온도·토양·강수·일조** 카테고리 점수 → **가중 종합 점수(0~100)** 산출.
- 점수순 정렬 → **추천 작물 랭킹** 제시(예: "이 지역 Top3: 감자 84 · 상추 81 · 사과 82").

### F-4. 추천 이유 설명 (P0)
- 각 작물의 **카테고리별 점수와 근거**를 자연어로 제시.
  - 예: "충주 칠금동은 강수·일조가 사과에 이상적(각 100점)이나, 토양 유효인산이 과다(14점)해 종합 82점."
- **감점 요인 → 개선 팁** 연결(예: "인산 시비 절감 시 토양 점수 개선 가능").

### F-5. 위험요소 안내 챗봇 (P1)
- 작물별 **생육 위험요소**를 질의응답. 데이터 근거는 `crop_standards_v2.json`.
  - 사과: 개화기 냉해 한계온도(만개기 −1.7℃ 등), 30℃ 이상 고온 장애.
  - 오이: 10℃ 이하 생육정지, 노균병(과습) 위험.
  - 감자: 25℃ 이상 덩이줄기 비대 정지, 더뎅이병(알칼리)·흑지병(과산성).
- 지역 데이터와 결합해 **"이 지역에서 특히 주의할 점"** 형태로 개인화.

### F-6. 농사 캘린더 (P1)
- 선택 작물의 **월별 농사 업무**(파종/정식·시비·관수·병해충 방제·수확)를 달력으로 제공.
- 지역 기후(예: 서리 종료일, 장마 시기)를 반영해 **시기 보정**.
- (P2) 일정 알림/푸시.

### F-7. 리포트 저장·공유 (P2)
- 지역별 진단 결과를 저장/PDF·링크 공유.

---

## 6. 데이터 소스 & API

| 데이터 | 출처 | 식별자/파라미터 | 비고 |
|---|---|---|---|
| 기상(기온·강수·일조) | 기상청 ASOS 일자료 (data.go.kr 15059093) | `stnIds`(관측소), `startDt`/`endDt` | 최근접 관측소 매핑 필요, 10년 평년 집계 |
| 토양 화학성 | 농진청 농경지화학성 통계 V2 (흙토람 SoilExamStat) | **`STDG_CD`=법정동코드(10자리)** | 성분별 operation(getFarmExam**Ph/Om/Ap/Kal/Cal/Mg/Sa**Info), 지목별 6등급 면적 분포 |
| 작물 표준 생육조건 | 농촌진흥청 농업기술길잡이 → `crop_standards_v2.json` | 작물명 | 온도/토양/강수/일조/시비 + 위험요소 기준 |
| 법정동코드 | 국토교통부 법정동코드 CSV | 이름→코드 (`bjd_lookup.py`) | 로컬, 키 불필요, 즉시 조회 |
| 지도/지역선택 | **네이버 지도 API** | 좌표·주소 | 프론트 지역 선택 UI |

> ⚠️ **키 관리**: 모든 서비스키는 `.env`(gitignore)에 보관. 소스/문서에 하드코딩 금지. (git 이력에도 유출 주의)

---

## 7. 적합도 점수 산출 로직 (핵심 알고리즘)

### 7.1 카테고리 & 중요도(가중치)
작물·정책에 따라 조정 가능. (사과 예시: 온도 45 / 토양 15 / 강수 23 / 일조 17, 합 100)

### 7.2 카테고리 점수 함수 (기상: 온도·강수·일조)
```
최적범위 안                → 100점
최적에서 near 이내로 벗어남 → 100 → 80점 (완만 감점)
near 초과                 → 80점부터 2배 빠르게 → 0점
  · near 예: 온도 2℃ / 강수 150mm / 일조 300h
```

### 7.3 토양 점수 함수
- 토양은 **등급별 면적 분포**로 제공됨 → 각 등급을 **작물 최적등급과의 거리**로 채점(최적±0.5등급=100, 1등급차=60, 2등급차↑=0) 후 **면적 가중 평균**.
- **등급→수치 경계표**는 공식 API 기술명세서 기준(지목별 상이). pH·유기물·유효인산은 밭·과수 경계 동일 → 과수 표본 희박 시 **밭 데이터 대용**.

### 7.4 종합 점수
```
종합 = Σ(카테고리 점수 × 중요도) ÷ 100
```

### 7.5 검증된 예시 (사과)
| 지역 | 온도 | 토양 | 강수 | 일조 | 종합 |
|---|--:|--:|--:|--:|--:|
| 충주 칠금동 | 76.7 | 50.8 | 100 | 100 | **82.1** |
| 강릉시 | 64.8 | 41.6 | 80.9 | 100 | **71.0** |

---

## 8. 시스템 아키텍처 / 데이터 플로우

```
[사용자]
  └─(네이버 지도)→ 지역 선택(좌표/주소)
        └─ bjd_lookup(CSV) → 법정동코드(STDG_CD)
        └─ 좌표 → 최근접 ASOS 관측소(stnId)
              ├─ 기상청 ASOS API ─┐
              ├─ 농진청 토양 API ─┤→ [스코어링 엔진] ─→ 작물별 점수·랭킹
              └─ crop_standards_v2.json ─┘        ├─ 추천 이유(자연어)
                                                  ├─ 위험요소 챗봇(LLM + 기준데이터)
                                                  └─ 농사 캘린더
```

### 성능 설계
- **캐싱**: ASOS 과거 일자료(불변)·토양 통계(연 1회 갱신)를 로컬/DB 캐시 → 재조회·다지역 비교 시 즉시 응답(실측 캐시 히트 시 ~0.1초, 콜드 대비 ~400배).
- **병렬 호출**: 연도별/성분별 요청을 동시 처리.
- **Keep-Alive**: HTTP 세션 재사용.

---

## 8-1. 기술 스택 (Tech Stack)

> 사용 언어·프레임워크: **Python · HTML · Next.js**

### 프론트엔드
| 영역 | 기술 | 용도 |
|---|---|---|
| 프레임워크 | **Next.js (React)** | 페이지 라우팅·SSR/CSR, 진단결과·캘린더·챗봇 UI |
| 마크업/스타일 | **HTML** + CSS (Tailwind 권장) | 화면 구조·반응형 레이아웃 |
| 지도 | **네이버 지도 API (JS SDK)** | 지역 선택·주소 검색·좌표 획득 |
| 데이터 통신 | fetch / axios | 백엔드(Python API) 호출 |

### 백엔드 (Python)
| 영역 | 기술 | 용도 |
|---|---|---|
| API 서버 | **Python** (FastAPI 권장) | 스코어링 엔진, 외부 API 오케스트레이션, 프록시(키 은닉) |
| 데이터 처리 | requests, (선택) pandas | ASOS·토양 API 호출·집계, 등급 매핑 |
| 스코어링 | 순수 Python 모듈 | 카테고리 함수·토양 등급거리·가중 종합 |
| 지역코드 | `bjd_lookup.py` | 이름→법정동코드(STDG_CD) 즉시 변환 |
| 챗봇 | **Claude API** (Python SDK) | 위험요소 Q&A(기준데이터 결합) |

### 데이터/인프라
| 영역 | 기술 | 용도 |
|---|---|---|
| 캐시 | 파일 or SQLite (→ 확장 시 Redis) | ASOS 과거값·토양 통계 캐싱 |
| 정적 데이터 | JSON/CSV | `crop_standards_v2.json`, 법정동코드 CSV |
| 비밀정보 | `.env` (gitignore) | 서비스키·엔드포인트 |
| 배포 | 프론트: Vercel(Next.js) / 백엔드: 컨테이너·서버 | — |

### 구성 원칙
- **키는 서버(Python)에서만 사용** — 프론트(Next.js)는 백엔드 프록시를 통해서만 외부 API 접근(클라이언트 키 노출 금지). 단, 네이버 지도 JS 키는 도메인 제한 설정 후 클라이언트 사용.
- **역할 분리**: Next.js = 화면/상호작용, Python = 데이터·스코어링·챗봇 로직.

---

## 9. 화면 흐름 (UX)

1. **홈/지도**: 네이버 지도에서 위치 선택 or 주소 검색.
2. **진단 결과**: 5종 작물 점수 카드 + Top 추천 강조. 카테고리별 막대(온도/토양/강수/일조).
3. **작물 상세**: 추천 이유, 감점요인·개선팁, 위험요소 요약.
4. **챗봇**: "이 지역에서 사과 키울 때 주의할 점?" 등 질의.
5. **캘린더**: 선택 작물 월별 업무.

---

## 10. 비기능 요구사항 (NFR)

| 구분 | 요구사항 |
|---|---|
| 성능 | 캐시 히트 시 진단 1초 이내, 콜드 시 관측소·토양 병렬로 수초 내 |
| 가용성 | 외부 API 장애(예: ASOS 게이트웨이 다운) 대비 **캐시 폴백 + 재시도 + 사용자 안내** |
| 보안 | API 키 `.env` 관리, 클라이언트 노출 금지(서버 프록시 경유) |
| 정확성 | 등급→수치 매핑은 공식 명세서 기준, 잠정치는 라벨 표기 |
| 확장성 | 작물/지역/가중치 파라미터화(설정으로 확장) |
| 접근성 | 모바일 우선, 디지털 리터러시 낮은 사용자 고려한 쉬운 문구 |

---

## 10-1. 비밀정보(.env) 관리 — 필수

> ⚠️ **유출되면 안 되는 모든 API 키·ID·시크릿은 반드시 `.env` 파일에 저장하고, 소스코드·문서·Git에는 절대 하드코딩하지 않는다.**

### .env에 넣어야 하는 값
| 항목 | 환경변수(예) | 유출 시 위험 | 노출 위치 |
|---|---|---|---|
| 공공데이터포털 서비스키(ASOS·토양 공용) | `DATA_GO_KR_SERVICE_KEY` | 무단 호출·트래픽 소진·계정 정지 | 서버(Python)만 |
| 네이버 지도 Client ID | `NAVER_MAP_CLIENT_ID` | 도메인 도용 | 클라이언트(도메인 제한 필수) |
| 네이버 지도 Client Secret | `NAVER_MAP_CLIENT_SECRET` | 지도/지오코딩 도용·과금 | 서버만 |
| Claude API 키(챗봇) | `ANTHROPIC_API_KEY` | 과금·오남용 | 서버만 |
| (선택) DB/캐시 접속정보 | `DATABASE_URL`, `REDIS_URL` | 데이터 유출 | 서버만 |

### 규칙
1. `.env`는 **`.gitignore`에 반드시 등록**(이미 `.env`, `.env.*` 무시 설정됨). 커밋 금지.
2. 저장소에는 **`.env.example`**(값은 빈칸/플레이스홀더)만 올려 필요한 키 목록을 공유.
3. **클라이언트(Next.js/HTML)에는 시크릿을 노출하지 않는다** — 외부 API는 **Python 백엔드 프록시** 경유. (네이버 지도 JS의 Client ID만 예외적으로 클라이언트 사용, 단 **도메인 제한** 설정 필수)
4. 문서(PRD·README·명세서)에 **실제 키 값을 적지 않는다.** 예시는 `<YOUR_SERVICE_KEY>` 같은 플레이스홀더로 표기.
5. 키가 **실수로 커밋·푸시된 경우 즉시 재발급(rotate)** 하고 이력에서 제거.

### `.env.example` 예시
```dotenv
# 공공데이터포털 (data.go.kr) — ASOS·토양 공용
DATA_GO_KR_SERVICE_KEY=<YOUR_SERVICE_KEY>
# 네이버 지도
NAVER_MAP_CLIENT_ID=<YOUR_CLIENT_ID>
NAVER_MAP_CLIENT_SECRET=<YOUR_CLIENT_SECRET>
# 챗봇 (Claude)
ANTHROPIC_API_KEY=<YOUR_ANTHROPIC_KEY>
```

---

## 11. 리스크 & 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| **ASOS/토양 API 백엔드 장애** (실제 발생: "Error forwarding request to backend server") | 진단 불가 | 캐시 폴백, 지수 백오프 재시도, "일시적 장애" 사용자 안내, 부분 결과(토양만) 제공 |
| data.go.kr **일일 트래픽 한도**(개발계정) | 조회 실패 | 운영계정 전환, 캐싱으로 호출 최소화 |
| **과수 등 지목 표본 희박** | 점수 신뢰도↓ | 밭 데이터 대용 + 신뢰도 표기, 시군구 단위 폴백 |
| 관측소-읍면동 **거리 편차**(산간 등) | 기온 오차 | 최근접+고도 보정 고려, 거리 표기 |
| 등급 경계·가중치의 **주관성** | 결과 논란 | 근거·가정 명시, 가중치 사용자 조정 옵션 |

---

## 12. 성공 지표 (KPI)

- 진단 완료율(지역 선택→결과 도달) ≥ 70%
- 추천 이유 "도움됨" 평가 ≥ 80%
- 챗봇 질의 해결률 ≥ 75%
- 캘린더 저장/알림 설정 비율(리텐션 지표)
- 재방문율(다른 지역/작물 재진단)

---

## 13. 마일스톤 (제안)

| 단계 | 범위 | 산출물 |
|---|---|---|
| **M1 (MVP)** | F-1~F-4 | 지도 선택 → 5종 점수·추천·이유 |
| **M2** | F-5 챗봇, 캐싱/병렬 최적화 | 위험요소 Q&A, 성능 개선 |
| **M3** | F-6 캘린더 | 월별 업무 + 지역 시기 보정 |
| **M4** | F-7, 알림, 계정 | 저장·공유·푸시 |

---

## 14. 부록 — 이미 구축/검증된 자산

- `crop_standards_v2.json` — 5종 작물 표준 생육지침(+위험요소 기준).
- `bjd_lookup.py` — 이름→법정동코드 즉시 조회(국토부 CSV).
- 스코어링 엔진 로직 — 카테고리 함수·토양 등급 매핑·가중 종합(충주·강릉 사과 검증).
- ASOS·토양 API 연동 + **병렬·캐싱** 최적화 스크립트(콜드 대비 캐시 ~400배).
- 토양 등급→수치 경계표(공식 명세서 기준, 지목별).
- `.env` — 서비스키·엔드포인트·조작명 정리(gitignore).
