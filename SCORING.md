# 홈화면 추천작물 적합도 산출 알고리즘

사용자가 홈화면에서 지역을 선택하면 작물 5종(사과·배·오이·감자·상추)의 적합도 점수와 순위가 뜬다.
이 문서는 그 점수가 **어떤 데이터로, 어떤 순서로, 어떤 수식으로** 만들어지는지를 설명한다.

관련 파일 한눈에:

| 역할 | 파일 |
|---|---|
| 프론트 화면·순위 표시 | `Beomin_web/CropAdvisor.dc.html` |
| HTTP API 서버 | `backend/crop_score_server.py` |
| 적합도 산출 본체 | `backend/scoring/climate_normal_score.py` |
| 가중치·근거값 원본 | `backend/scoring/reference_data.py` |
| 점수 변환 수식 | `backend/scoring/scoring_engine.py` |
| 지역→관측소 매칭 | `backend/utils/region_mapper.py`, `backend/utils/crop_station_registry.py` |
| 토양 데이터 조회 | `backend/api/soil.py`, `backend/api/soil_ec.py` |
| 미리 계산된 평년 점수 | `data/processed/climate_normal_scores.json` |

---

## 0. 설계 전제 — 왜 "실시간"이 아니라 "평년"인가

홈화면 적합도는 **접속 시점과 무관해야 한다.**

사용자가 알고 싶은 건 *"이 지역에서 이 작물 키우는 게 얼마나 쉬운가/어려운가"*지,
*"오늘 날씨가 어떤가"*가 아니다. 게다가 사용자가 귀농 전인지 후인지, 언제 접속했는지 알 수 없다.

실시간 기상 데이터를 쓰면 같은 지역·같은 작물인데도 폭염 때 조회하면 "위험", 평소에 조회하면 "우수"가 나온다.
그래서 홈화면은 **여러 해 통계로 미리 계산해둔 고정값**만 쓴다.

> 실시간 점수 경로(`/api/crop-score/`, `live_scoring.py`)는 지금도 남아 있지만 홈화면 적합도에는 쓰지 않는다.

---

## 1. 전체 흐름

```
사용자가 지역 선택 (예: "강원도 평창군 대관령면")
        │
        ▼
[프론트] fetchScores()  ─ 작물 5종을 병렬로 동시 요청
        │
        ▼
GET /api/crop-score-normal/{작물}?region={지역명}
        │
        ▼
[서버] build_normal()  ─ 10분 메모리 캐시
        │
        ▼
[본체] get_climate_normal_score(region, crop)
        │
        ├─ ① 지역 → 이 작물의 근거 관측소 매칭
        ├─ ② 기후 3종(온도·강수·일조) 점수 ← 미리 계산된 JSON에서 조회
        ├─ ③ 토양 4종(pH·유기물·유효인산·EC) 점수 ← 흙토람 API 실시간 조회
        ├─ ④ 결측 항목 제외 + 가중치 재정규화
        └─ ⑤ 가중합 → 총점
        │
        ▼
[프론트] 총점으로 등급 판정 → 점수 내림차순 정렬 → 1~5위 표시
```

---

## 2. 단계별 상세

### ① 지역 → 관측소 매칭

지역명을 좌표로 바꾼 뒤, **그 작물의 근거값이 실제로 존재하는 관측소** 중에서 최근접을 고른다.
전국 89개 관측소 중 아무거나가 아니다. (`crop_station_registry.py`)

| 작물 | 근거 관측소 |
|---|---|
| 사과 | 영주, 안동, 문경, 거창 |
| 배 | 천안, 광주 |
| 오이 | 순천·진주·밀양(촉성재배), 부여·대전(반촉성재배) |
| 감자 | 서산·밀양(봄재배), 대관령(고랭지재배) |
| 상추 | 대관령·태백·정선군(고랭지재배), 남해(저지대재배) |

- 매칭된 관측소가 **80km보다 멀면** `station_warning`("근거 관측소와 거리가 멀어 정확도가 낮을 수 있습니다")이 붙는다.
- 이때 **재배형태(cultivation_type)도 함께 결정된다.** 예를 들어 감자는 대관령에 매칭되면 "고랭지재배", 서산이면 "봄재배"가 되고, 이후 모든 근거값(온도 임계치·생육기간 등)이 그 재배형태 기준으로 바뀐다.
- 별도로 일반 최근접 관측소도 조회해 **기후 클러스터(6종)** 정보를 붙인다. 이건 점수 계산에는 안 쓰이고, 프론트에서 지역 설명 문구를 고르는 데만 쓴다.

### ② 기후 3종 점수 (온도·강수·일조)

`data/processed/climate_normal_scores.json`에서 `"{작물}|{관측소}|{재배형태}"` 키로 조회한다. **API 호출 없음.**

```json
"감자|대관령|고랭지재배": {
  "years_used": 10,
  "온도_score": 86.5, "강수_score": 99.4, "일조_score": 100.0,
  "강수_avg_mm": 936.6, "일조_avg_hr": 1055.3
}
```

#### ⚠️ 이 JSON을 만든 방식이 이 알고리즘의 핵심

near/danger 임계치는 원래 **"과거 관측값들의 평균 ± 표준편차"**로 만들어졌다.
그래서 **평년값(평균) 자체를 그 임계치와 비교하면 수학적으로 항상 100점이 나온다.**
그 평균으로 임계치를 만들었으니 당연히 안전한 쪽에 있기 때문이다.

**틀린 방법:** 10년치 값을 평균 → 그 평균 하나를 채점 → 항상 100점 (지역 구분 불가)
**쓴 방법:** 10년치를 **연도별로 각각 채점** → 그 **점수들을 평균**

이렇게 하면 "10년 중 몇 번이나 위험했나"가 점수에 반영돼서 지역 간 차이가 실제로 벌어진다.

실제 결과 (사과 온도점수): 안동 10.0 / 거창 22.1 / 문경 27.5 / 영주 28.3
→ 같은 사과라도 관측소별로 확실히 구분된다.

| 항목 | 데이터 | 기간 |
|---|---|---|
| 온도 (오이·감자·상추) | ASOS 일자료 최저/최고기온 | 10년 |
| 온도 (사과·배) | 로컬 시간단위 기온 + 지속시간 규칙 | 6년 |
| 강수·일조 | ASOS 일자료 생육기간 누적 | 10년 |

> 사과·배 온도만 6년인 이유: 시간단위 데이터가 2020년부터만 로컬에 있어서다.
> 사과·배는 "0℃가 48시간 지속" 같은 **지속시간 조건**이 있어 일자료로는 판정할 수 없다.

### ③ 토양 4종 점수 (pH·유기물·유효인산·EC)

흙토람 API를 그때그때 조회한다. 토양 통계는 애초에 "누적 검정 통계"라 실시간성이 없어서 평년화가 필요 없다.

**지목(land-use) 대체 로직** — 작물마다 조회하는 지목이 다르다:

| 작물 | 지목 |
|---|---|
| 사과·배 | 과수원(Fruit) |
| 오이 | 시설재배지(Fachs) |
| 감자·상추 | 밭(Pfld) |

문제는 지역에 따라 특정 지목의 등록 필지가 **0건**인 경우가 실제로 있다는 것이다.
(예: 서울 중랑구는 시설재배지 0건 → 오이만 토양 3종이 통째로 결측)

같은 지역인데 작물마다 데이터 유무가 들쭉날쭉해지므로, **원래 지목이 0건이면 다른 지목 값으로 대체**한다.
대체 순서는 밭 → 과수원 → 시설재배지 → 논. 흙토람 API가 애초에 지목 4개를 한 번에 다 주기 때문에 **추가 API 호출은 없다.**

대체된 항목은 응답에 `note`가 붙고, 프론트에서 **해당 막대에 마우스를 올렸을 때만** 툴팁으로 안내한다
(항목명에 점선 밑줄로 힌트만 표시).

> EC만 다른 API(`getSoilExamList`)를 쓴다. 지목 개념이 없고, 읍면동별 실측 시료의 EC를 평균낸다.
> 이 API는 하루 호출한도가 빡빡해서 429가 자주 나며, 그 경우 EC는 결측 처리된다.

### ④ 결측 처리 + 가중치 재정규화

**작물별 가중치** (`WEIGHT_MATRIX`, 행별 합=100):

| 작물 | 온도 | 강수 | 일조 | pH | 유기물 | 유효인산 | EC |
|---|---|---|---|---|---|---|---|
| 사과 | 38 | 22 | 18 | 6 | 4 | 4 | 8 |
| 배 | 40 | 22 | 18 | 5 | 3 | 4 | 8 |
| 오이 | 30 | 10 | 25 | 6 | 3 | 4 | **22** |
| 감자 | 28 | 15 | **32** | 7 | 3 | 4 | 11 |
| 상추 | **35** | 10 | 25 | 8 | 3 | 3 | 16 |

작물마다 중요한 변수가 다르다 — 오이는 염류(EC)에 민감하고, 감자는 일조가 결정적이고, 사과·배는 온도(냉해·일소)가 압도적이다.

**재정규화:** 결측된 변수의 가중치를 남은 변수들에게 **비율대로 나눠준다.**

```
예) 감자에서 유기물(3)·EC(11)가 결측 → 남은 가중치 합 = 86
    온도 28 → 28/86 × 100 = 32.56
    일조 32 → 32/86 × 100 = 37.21   ... (합계 100)
```

`weight_coverage`(위 예시에선 86.0)로 "원래 가중치의 몇 %를 실제로 썼는지"를 함께 반환한다.
결측 항목이 있으면 프론트에 "일부 항목 데이터가 없어 남은 항목만으로 계산됐어요" 안내가 뜬다.

### ⑤ 총점

```
총점 = Σ (변수별 점수 × 재정규화 가중치 ÷ 100)
```

---

## 3. 변수별 점수 수식

모든 변수는 0~100점으로 환산된다. 변수마다 "위험한 방향"이 달라서 3가지 유형으로 나뉜다.

### 유형 A — 양방향 (온도)

너무 추워도, 너무 더워도 위험하다.

```
   100 ┤      ┌──────────────┐
       │     ╱                ╲
    30 ┤   ╱                    ╲
     0 ┤ ╱                        ╲
       └─┴───┴──────────────┴───┴──→
      냉해   냉해            폭염  폭염
      위험   near            near  위험
```

- `냉해near ≤ 값 ≤ 폭염near` → **100점**
- near를 벗어나면 위험값까지 **100→30점 선형 감점**
- 위험값을 넘어도 **같은 기울기로 계속 하락** (0점 바닥) — 절벽 없이 부드럽게

임계치는 작물·재배형태별로 다르다. 예: 감자 고랭지재배는 냉해near −17.75℃지만, 봄재배는 −7.70℃다.

**사과·배는 예외** — 재해보험 기준의 "온도 × 지속시간" 조합 규칙을 쓴다.
"0℃가 48시간 지속" / "33℃가 2일 지속" 같은 조건이라 순간 기온만으로는 판정할 수 없고,
시간단위 시계열을 넣어 `temperature_duration_rule.py`가 개화기 냉해·일소를 정밀 판정한다.

### 유형 B — 적을수록 위험 (강수·일조)

생육기간 **누적값**이 기준이다. (예: 사과 4/1~10/31, 감자 고랭지재배 5/1~9/30)

- `값 ≥ near` → **100점**
- near 아래로는 위험값까지 100→30점 선형 감점, 이후 같은 기울기로 계속 하락

### 유형 C — 범위 이탈 (pH·유기물·유효인산)

"높으면 위험"이 아니라 **적정범위를 벗어나면** 위험하다. 위험값 개념이 없어 사실상 이분법이지만, 절벽을 피하려고 완충구간을 둔다.

```
   100 ┤    ┌────────────┐
       │   ╱              ╲
    40 ┤──┘                └──────
       └──┴─┴──────────┴─┴───────→
        완충 min       max 완충
        (범위폭의 10%)
```

- 적정범위 안 → **100점**
- 범위 밖 → 범위폭의 10%만큼 완충구간에서 100→40점, 그 밖은 **40점 고정**

예: 감자 pH 적정 5.5~6.2, 상추 6.5~7.0 / 오이 유효인산 400~500 mg/kg, 감자 250~350

### 유형 D — 높을수록 위험 (EC)

- `값 ≤ near` → **100점**
- 위험값이 있으면(감자만) near~위험값 선형 감점
- 위험값이 없으면 `near × 1.3`을 근사 위험값으로 잡아 감점

---

## 4. 프론트 표시 규칙

### 등급

총점을 4단계로 나눈다. 서버(`grade_of`)와 프론트(`normalGradeOf`)가 동일 기준을 쓴다.

| 총점 | 등급 | 라벨 | 색 |
|---|---|---|---|
| 80 이상 | good | 우수 | 진초록 |
| 60~79 | normal | 양호 | 초록 |
| 40~59 | caution | 주의 | 주황 |
| 40 미만 | bad | 위험 | 빨강 |

### 순위

총점 **내림차순**으로 정렬해 카드에 1~5위 동그라미를 붙인다.
그리드 레이아웃이라 순서만으로는 "적합도 순"이라는 게 잘 안 읽혀서 순위를 명시한다.

### 항목별 점수 막대

`breakdown`을 온도 → 강수 → 일조 → pH → 유기물 → 유효인산 → EC 순으로 표시한다.
막대 색은 점수 구간별로 바뀌고(80↑ 진초록 / 60↑ 연초록 / 40↑ 주황 / 그 미만 빨강),
결측 항목은 아예 막대가 안 나온다.

### 폴백 (총점을 못 구했을 때)

1. **기후 클러스터 코멘트** — 6개 클러스터별로 미리 써둔 작물별 문구 (좋음 85 / 보통 65 / 주의 45점으로 정렬)
2. **5권역 표(CROP_ZONE)** — 클러스터 정보조차 없을 때. "참고 등급" 안내가 붙는다
3. **"정보 없음"**

---

## 5. 알려진 한계

| 한계 | 설명 |
|---|---|
| **흙토람 API 호출한도** | pH·유기물·유효인산·EC가 429(한도 초과)로 결측되는 일이 잦다. 429 재시도 2회 + 디스크 캐시(성공 24h/실패 3h)로 완화했지만, 하루 한도 자체는 코드로 우회 불가. ASOS API와 키를 공유해서 더 빨리 소진된다 |
| **사과·배 온도 6년** | 시간단위 데이터가 2020년부터만 있어 10년으로 못 늘렸다 |
| **관측소 거리** | 근거 관측소가 5종 합쳐 15곳뿐이라 먼 지역은 80km 경고가 뜬다 (예: 서울에서 오이 → 대전, 136km) |
| **생육기간 추정** | `GROWTH_PERIOD`의 월 구분은 일반 농업기술 지식 기반 추정이며, 실제 파종·수확일로 검증되지 않았다 |
| **지목 대체는 근사** | 시설재배지 대신 밭 값을 쓰면 실제와 다를 수 있다(시설은 인산 축적이 노지보다 훨씬 심하다). 참고용임을 툴팁으로 안내한다 |

---

## 챗봇 적합도도 평년으로 통일 (2026-08-05)

heeyeon2026 병합 직후, **화면은 평년인데 챗봇만 실시간**이었다. 챗봇의
`get_crop_score` 도구가 `/api/crop-score`(단기예보 온도 + 올해 누적 ASOS 강수·일조)를
계속 부르고 있어서, 같은 지역·작물을 물어도 두 숫자가 갈렸다. 폭염·장마 중에는 차이가
커져 사용자가 "화면은 우수인데 챗봇은 주의라던데?"를 묻게 된다.

`get_crop_score`를 `/api/crop-score-normal`로 바꿨다. 응답 형태가 달라서 함께 손봤다.

| 실시간 응답 | 평년 응답 |
|---|---|
| `reliability` / `reliability_reason` | `weight_coverage`(반영된 가중치 %) + `excluded_variables` |
| — | `cluster_name` · `years_used` · `cultivation_type` · `station_warning` |
| `breakdown.{score,weight}` | `breakdown.{score,weight,**source**}` |

`source`를 그대로 넘긴다 — 온도·강수·일조는 `평년(다년 평균 채점)`, 토양은
`흙토람(누적 통계)`이라 원천이 다르다. 근거를 물으면 항목별로 답해야 한다.

### 연쇄 영향 하나를 함께 고쳤다

`/api/cultivar-score`가 "감자 93.7점 → 그중 추백 91.0점"으로 잇도록 작물 점수를 함께
싣는데, 그 값을 **실시간 캐시(`_cache`)에서 읽고 있었다.** 아무도
`/api/crop-score`를 부르지 않게 된 뒤로는 늘 비어 있어 작물점수가 아예 실리지 않았다.
`build_normal()`을 직접 호출하도록 바꿨다(미리 계산된 JSON만 읽어 빠르다).

### 검증 (로컬 8003 · 실제 Opus 5 호출)

| 질문 | 결과 |
|---|---|
| "충주에서 감자 키우면 어때요?" | 93.7점 우수 · 항목별 근거 · 관측소(대관령) 거리 주의 · 평년 10년 명시 |
| "그 점수는 오늘 날씨 기준인가요?" | "여러 해 평년 통계 기준이라 오늘 날씨와 상관없이 항상 같은 값" + 항목별 원천 구분 |
| "요즘 충청북도 날씨는?" | `get_weather` 호출 · "이 실측 날씨는 적합도 점수와는 별개예요" |
| "충주 사과는 왜 점수가 낮아요?" | 온도 28점·비중 38%로 원인 설명 · 평년 10년·문경 관측소 명시 |

도구 응답 587자(이전보다 작다). 남은 실시간 `crop-score` 호출 0건.

### 아직 실시간인 것 (의도적)

- `get_weather` — 최근 14일 실측. "오늘 무엇을 할까"가 목적이다.
- 화면의 7일 예보 준비 카드 · 수분 상태 — 같은 이유.
- `/api/crop-score`(실시간) 라우트는 남겨 뒀다. 부르는 곳은 없지만 비교·검증용으로 쓴다.
